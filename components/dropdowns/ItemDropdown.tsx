import classNames from 'classnames';

export const ItemDropdown = () =>
    (
        <div
            className={classNames(
                'absolute top-8 right-4 h-80 w-32 border border-2 border-black bg-white shadow'
            )}
        >
          <p>item</p>
        </div>
    );

